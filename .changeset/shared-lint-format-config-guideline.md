---
'@pair/knowledge-hub': minor
---

Add shared-config-packages guideline (per-type overrides, `tools/*` reference) and extend `/pair-capability-setup-gates` with a provisioning step for shared lint/format configs + husky pre-commit/pre-push hooks (KB default, adoption-overridable).
