---
"@pair/knowledge-hub": minor
"@pair/website": minor
---

Add the context map as an optional, inline-maintained artifact: dispatcher + index + core template for the domain glossary/entities/common-rules, with a lazy per-subdomain split into `subdomain/<slug>.context.md` once a subdomain's inline context grows. Wired into `/pair-process-refine-story` requirements analysis (domain check + conflict-flag). Mirrors on the docs site at `/docs/concepts/adoption-files`.
