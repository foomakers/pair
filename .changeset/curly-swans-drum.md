---
"@pair/pair-cli": minor
---

feat: `pair-cli run --card <id> [--pr] [--rounds] [--run-id]` — console-side serial delivery-cycle coordinator, dispatching pi/opencode/claude headlessly through the same stages `/pair-workflow-cycle` drives in-session (#487)
