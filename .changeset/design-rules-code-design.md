---
"@pair/knowledge-hub": minor
---

Add `design-rules.md` (do/don't rules, evidence-linked to #199, migration plan) to code-design guidelines. Reference the rules from `/pair-process-implement` (generation-time constraint) and `/pair-process-review` (violation must cite rule ID; ambiguous match is a suggestion, not a violation).
