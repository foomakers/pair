{
  "$meta": {
    "pr": "494",
    "phase": "r2-g3",
    "base": "eb73d179738e8b05da8bb27ecfdf7b554a33fdea",
    "trailer": "Pair-RED-Snapshot: pr=494; phase=r2-g3; base=eb73d179738e8b05da8bb27ecfdf7b554a33fdea; manifest=.pair/red-snapshots/pr-494-r2-g3.json",
    "artifacts": [
      "packages/dev-tools/src/quality-gates/ci-staleness-checkout-depth.test.ts"
    ]
  },
  "schema": "red-contract/1",
  "run": "story-486",
  "story": "486",
  "pr": 494,
  "branch": "feature/US-486-pair-workflow-cycle-coordinator",
  "phase": "r2-g3",
  "mode": "repair",
  "attempt": 2,
  "remediationBatchId": "r2",
  "groupId": "r2-g3",
  "workflowVersion": "4.0.1",
  "inputHead": "eb73d179738e8b05da8bb27ecfdf7b554a33fdea",
  "base": "origin/main",
  "repairOf": {
    "contractHash": "sha256:30a9608786998271326b87261e7fa3bd06bcf55f3c9a6c1b5649bcb196630785",
    "rejection": [
      "r2-g3-v1"
    ],
    "rowIds": [],
    "mechanismIds": []
  },
  "phaseRoutingNote": "resolve() at this head returned next.phase='r2-g1' (round 2, attempt 1) for finding r1-3, computed by cycle-state.mjs's review-phase fallback (`r${round+1}-g1`, round derived from the LAST review handoff's own phase name 'r1-review-phase' -> round 1) because r1-3 carries no groupId for the review-phase branch to route on. That literal phase would (a) collide with and overwrite the ALREADY-SEALED r2-g1-red-contract.json (contractHash sha256:15ec825cea6524ab705063438cad13db403ae213f482fdacb37f7c5fc592e1f8, verified:true, sealed:true, fixing a DIFFERENT finding r1-1) and (b) contradict the engine's own green-fix->nextGroup mechanism, which -- had the round-2 review not been mislabeled 'r1-review-phase' -- would have advanced planFor(2).groups from r2-g2 to groups[2]='r2-g3' automatically (cycle-state.mjs ~L1780-1786, orderGroups(planFor(round).groups), nextGroup = groups[idx+1]). r2-g1-red-contract.json's own sealed plan.groups already fully specifies group 'r2-g3' (findings:['r2-g1-rem1'], mode:'behavioral', allowedPaths:['.github/workflows/release.yml'], oracle naming this exact test file and assertion, dependsOn:['r2-g1']) for precisely the obligation review re-discovered independently as 'r1-3' (identical location, identical recommendation: 'add fetch-depth: 0 to .github/workflows/release.yml's checkout'). No r2-g3-* handoff exists in this run directory prior to this one. Given the sealed-contract-overwrite would be an irreversible custody violation and the pre-planned group is fully specified and unconsumed, this preparation is filed under the already-planned groupId 'r2-g3' rather than the literal (colliding) phase string resolve() returned. Escalated back to the launching agent in the same turn for confirmation; not silently substituted.",
  "sourceOfTruth": "apps/website/lib/docs-staleness-check.ts:612-623 (newestChangeDate, the sole date resolver) + .github/workflows/release.yml job `release`'s single actions/checkout@v4 step (line 29-30, no `with:`, so GitHub Actions applies its default depth-1), which reaches the gate through root package.json's `quality-gate` chain (step 59: `run: pnpm quality-gate`). packages/dev-tools/src/quality-gates/ci-staleness-checkout-depth.test.ts's own gateRunners/gateRunnersWithoutHistory helpers, already authored by r2-g1 and unchanged in their derivation logic, are the oracle: they parse every real workflow document and the real root package.json script closure, never a stubbed twin.",
  "status": "red",
  "testExempt": false,
  "scope": {
    "inheritedFrom": "r2-g1-red-contract.json plan.groups[groupId='r2-g3']",
    "groupId": "r2-g3",
    "findings": [
      "r2-g1-rem1",
      "r1-3"
    ],
    "dependsOn": [
      "r2-g1"
    ],
    "dependsOnSatisfied": "r2-g1-red-verify.attempt-2.json: verified true, sealed true"
  },
  "inventory": [
    {
      "id": "r1-3",
      "aliasOf": "r2-g1-rem1",
      "aliasRationale": "Same producer, same location, same defect, same recommendation, discovered twice: once by r2-g1's own preparation (raised as obligation r2-g1-rem1, group r2-g3 planned then) and independently by the round's review (raised as r1-3, missedUpstream:true, because r2-g3 was never dispatched). This contract discharges both under one row set.",
      "producer": ".github/workflows/release.yml job `release` -- its single `actions/checkout@v4` step (line 29-30) declares no `fetch-depth`, so the job runs at the GitHub Actions default depth (1) while step 59 (`run: pnpm quality-gate`) reaches `pnpm docs:staleness` through the root package.json script chain (package.json line 34). It is the second live member of class C6 (ci-staleness-checkout-depth.test.ts's own derivation) and lies outside r2-g1's fixScope.allowedPaths, which is exactly why it was carried forward as its own obligation instead of being fixed there.",
      "inputs": [
        ".github/workflows/release.yml -- the checkout declaration of job `release`",
        "root package.json scripts.quality-gate -- the chain that makes the job a gate runner",
        "the CLONE the release job runs against: GitHub Actions' default shallow checkout (depth 1) today, full history after this group's fix"
      ],
      "representations": [
        "the derived global offender set: gateRunnersWithoutHistory(readWorkflows(), scriptsReachingTheGate()) -- contains '.github/workflows/release.yml:release' at this base",
        "the release pipeline result: the `Run quality checks` step of job `release` (tag push, `release: published`, or `workflow_dispatch`)",
        "a real `pnpm docs:staleness` run inside a genuine `git clone --depth 1` of this exact head: FAIL, 'cannot resolve the last change date of packages/knowledge-hub/dataset/.skills/ from git -- the catalog freshness check cannot run', exit 1"
      ],
      "consumers": [
        "the release / tag-push / workflow_dispatch publish pipeline",
        "every consumer of r1-1's fix, through the interaction r2-g1's contract named as I5 (r1-1's correction makes this member's failure deterministic instead of calendar-dependent)"
      ],
      "classes": [
        "C6-ii (shared with r2-g1's inventory) -- the release runner at the default depth. THE DEFECT, live at this head.",
        "C5-ii -- the same runner declaring fetch-depth: 0. The target state; zero members today."
      ],
      "interactions": [
        "I5 (shared with r2-g1's r1-1 row) -- this group's fix discharges the accepted interim outcome r2-g1 named (r2-g1-ao1): the release pipeline's deterministic hard stop was accepted ONLY because this obligation would close it in the same round."
      ]
    }
  ],
  "fixScope": {
    "owner": "the checkout of the SECOND derived gate runner -- .github/workflows/release.yml job `release`, whose only checkout (actions/checkout@v4, line 29-30) declares no fetch-depth while step 59 runs `pnpm quality-gate`, which the root script chains into `pnpm docs:staleness`",
    "mode": "behavioral",
    "allowedPaths": [
      ".github/workflows/release.yml"
    ]
  },
  "matrix": [
    {
      "id": "r2-g3-w1",
      "kind": "witness",
      "baseline": "red",
      "condition": "the GLOBAL derived offender set (no OWNED_WORKFLOWS filter) -- every gate-running job in the repository, not only the ones a prior group owned, must check out full history",
      "oracle": "cd packages/dev-tools && pnpm exec vitest run src/quality-gates/ci-staleness-checkout-depth.test.ts -t 'checks out full history in every gate-running job in the repository, not only the ones a prior group owned'",
      "expected": "gateRunnersWithoutHistory(readWorkflows(), scriptsReachingTheGate()) === []",
      "covers": [
        "r1-3",
        "r2-g1-rem1"
      ],
      "rationale": "Re-uses the real gateRunners/gateRunnersWithoutHistory derivation r2-g1 already authored and proved discriminant (r2-g1-v1) -- no filter to OWNED_WORKFLOWS this time, so the release-side member the prior group's own scope excluded is exactly what turns this row red. At this base the set is ['.github/workflows/release.yml:release'] (verified live: `node` probe run against the real workflow files at this head); it becomes [] only when release.yml's checkout also declares fetch-depth: 0."
    },
    {
      "id": "r2-g3-c1",
      "kind": "control",
      "baseline": "pass",
      "condition": "the release job is a gate runner (through the quality-gate chain) and has exactly one checkout step -- the mechanism this fix must touch, pinned so the witness cannot be emptied by removing or renaming that step instead of fixing it",
      "oracle": "cd packages/dev-tools && pnpm exec vitest run src/quality-gates/ci-staleness-checkout-depth.test.ts -t 'confirms the release job runs the gate through the quality-gate chain and has exactly one checkout step to fix'",
      "expected": "gateRunners(...) contains '.github/workflows/release.yml:release' AND the job has exactly 1 checkout step",
      "covers": [
        "r1-3"
      ],
      "rationale": "Positive/mechanism-pin control: passes today and must keep passing after the fix, since the fix only adds `with: fetch-depth: 0` to the existing step, never removes or replaces it."
    }
  ],
  "redTests": [
    {
      "file": "packages/dev-tools/src/quality-gates/ci-staleness-checkout-depth.test.ts",
      "kind": "test",
      "baseline": "red",
      "sha256": "sha256:cb6e9b7a77ef21f608e9f322bb2a58205cff8d2eb869ad9c5c33ada215d8b6c2",
      "command": "cd packages/dev-tools && pnpm exec vitest run src/quality-gates/ci-staleness-checkout-depth.test.ts",
      "observed": "Test Files 1 failed (1); Tests 1 failed | 6 passed (7). Failing: 'checks out full history in every gate-running job in the repository, not only the ones a prior group owned' -- AssertionError: expected [ '.github/workflows/release.yml:release' ] to deeply equal []. Passing: all 6 pre-existing rows plus the new control 'confirms the release job runs the gate through the quality-gate chain and has exactly one checkout step to fix'."
    }
  ],
  "supportingEvidence": [
    {
      "id": "r2-g3-ev1",
      "description": "Live reproduction at inputHead eb73d179738e8b05da8bb27ecfdf7b554a33fdea, run fresh in this preparation (not carried from an earlier head): a genuine `git clone --depth 1` of the branch, simulating exactly what .github/workflows/release.yml's checkout produces today.",
      "shallow": {
        "command": "git clone --depth 1 file:///Users/gianlucacarucci5/projects/pair-worktrees/486 <tmp> && DOCS_STALENESS_ROOT=<tmp> pnpm exec tsx apps/website/lib/docs-staleness-check.ts",
        "observed": "FAIL -- 1 issue: 'cannot resolve the last change date of packages/knowledge-hub/dataset/.skills/ from git -- the catalog freshness check cannot run'. exit 1."
      },
      "afterUnshallow": {
        "command": "git -C <tmp> fetch --unshallow origin && DOCS_STALENESS_ROOT=<tmp> pnpm exec tsx apps/website/lib/docs-staleness-check.ts",
        "observed": "PASS -- 51 skills, 11 commands in sync. exit 0."
      },
      "note": "Demonstrates the exact behavior release.yml's checkout produces before and after the fix this group's fixScope authorizes; not itself the sealed row (the sealed row is r2-g3-w1, which reads the real workflow YAML declaration, since a CI run cannot be executed from red-spec)."
    }
  ],
  "reconciled": [
    {
      "path": "packages/dev-tools/src/quality-gates/ci-staleness-checkout-depth.test.ts",
      "sha256": "sha256:cb6e9b7a77ef21f608e9f322bb2a58205cff8d2eb869ad9c5c33ada215d8b6c2"
    }
  ],
  "preserved": []
}
