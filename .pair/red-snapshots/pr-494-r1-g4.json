{
  "$meta": {
    "pr": "494",
    "phase": "r1-g4",
    "base": "ed3318f4a94c4d76a11d9673d6b76f4b41ef62b1",
    "trailer": "Pair-RED-Snapshot: pr=494; phase=r1-g4; base=ed3318f4a94c4d76a11d9673d6b76f4b41ef62b1; manifest=.pair/red-snapshots/pr-494-r1-g4.json",
    "artifacts": [
      "packages/knowledge-hub/src/tools/workflow-mirror.test.ts"
    ]
  },
  "run": "story-486",
  "story": "486",
  "pr": 494,
  "branch": "feature/US-486-pair-workflow-cycle-coordinator",
  "phase": "r1-g4",
  "mode": "remediation",
  "status": "red",
  "groupId": "r1-g4",
  "remediationBatchId": "r1",
  "inputHead": "ed3318f4a94c4d76a11d9673d6b76f4b41ef62b1",
  "inputsDigest": "424c8d45c92b3fef",
  "attempt": 1,
  "worktree": "/Users/gianlucacarucci5/projects/pair-worktrees/486",
  "testCommand": "pnpm --filter @pair/knowledge-hub exec vitest run src/tools/workflow-mirror.test.ts",
  "formatGate": "pnpm prettier:fix run over the artifact before hashing; pnpm prettier:check exits 0 at this head",
  "sourceOfTruth": "The two REAL directory trees — .claude/workflows/ and packages/knowledge-hub/dataset/.workflows/ — read by listFiles() at head ed3318f4a94c4d76a11d9673d6b76f4b41ef62b1. The root-only set is DERIVED from them (installed minus dataset), never asserted from prose; the exclusion policy is the enumeration recorded beside the guard in packages/knowledge-hub/src/tools/workflow-mirror.test.ts, which this contract turns from a comment into that derivation's expected value.",
  "inventory": [
    {
      "id": "r0-7",
      "producer": "The root-only exclusion policy recorded in packages/knowledge-hub/src/tools/workflow-mirror.test.ts beside the mirror guard (base lines 71-94) — the sole authority on which files may exist under .claude/workflows/ with no dataset counterpart. Its subject, the ACTUAL root-only set, is produced by listFiles('.claude/workflows') minus listFiles('packages/knowledge-hub/dataset/.workflows') over the real trees; the policy is the only thing that says which members of that set are deliberate.",
      "inputs": [
        "the real tree under .claude/workflows/ (21 files at this head)",
        "the real tree under packages/knowledge-hub/dataset/.workflows/ (18 files at this head)",
        "the enumerated policy itself (path -> rationale)",
        "the module specifiers .claude/workflows/pair-contracts/cycle-coordinator.test.mjs resolves relative to itself (../../skills/pair-workflow-*), and the paths they would denote from the dataset location"
      ],
      "representations": [
        "BASE: a prose comment inside the 'the root copy carries every dataset file' test (lines 71-94) — the only record before this contract, unexecutable, and naming only pair-analyze-pr-batch.js / .test.mjs",
        "the real root tree .claude/workflows/ (the set the policy must describe)",
        "the real dataset tree packages/knowledge-hub/dataset/.workflows/",
        "cycle-coordinator.test.mjs's own header ('RUNS FROM `.claude/workflows` ONLY: the ../../skills/pair-workflow-* imports resolve nowhere else')",
        "apps/pair-cli/config.json asset_registries.workflows.exclude — a DIFFERENT list (install-exclusion of DATASET *.test.mjs), already guarded at base lines 198-218; it is not the root-only policy and can never name a root-only file"
      ],
      "consumers": [
        "a contributor reading the enumerated list to decide whether a root-only file is deliberate policy or drift someone forgot to mirror — the finding's actual failure mode",
        "the byte-identity guard in the same suite (it goes GREEN on a wrong mirror, so it cannot catch this)",
        "pnpm workflows:test = `cd .claude/workflows && node --test` — the only place cycle-coordinator.test.mjs is ever executed",
        "scripts/regenerate-mirrors.sh, which covers .pair/knowledge/**, .github/agents/**, .github/workflows/** and NOT .claude/workflows <-> dataset/.workflows: mirroring this pair is a manual human act, which is why the written policy is the whole defence"
      ],
      "classes": [
        "C1 — a file present in BOTH copies (18 of them): mirrored, not root-only, outside the policy",
        "C2 — a root-only file WITH a recorded rationale: pair-analyze-pr-batch.js and pair-analyze-pr-batch.test.mjs (permitted, and the base records why)",
        "C3 — a root-only file WITHOUT a recorded rationale: pair-contracts/cycle-coordinator.test.mjs at the base. THE DEFECT: the reader sees an enumerated list of two and a third file that is in neither list",
        "C4 — a recorded rationale for a file that is NOT (or no longer) root-only: a stale entry that protects nothing while reading as authority",
        "C5 — a rationale recorded but empty or too thin to act on: the enumeration is complete and still useless",
        "C6 — a rationale whose stated reason has gone stale: it claims imports that cannot resolve from the dataset, and one of them now can",
        "C7 — a mirror pair whose permitted root-only set is EMPTY (agent definitions): a root-only file there is drift, with no exemption to hide behind",
        "C8 — a dataset file missing at the root: real drift, the direction the base guard already covers and which must keep working"
      ],
      "interactions": [
        "I1 C3 x the byte-identity guard — the repair a reader reaches for when the list is silent (copy the file into dataset/.workflows/) leaves the subset guard AND the byte guard green, because the two copies are then equal; nothing in the suite objects, and the dataset copy resolves none of its imports",
        "I2 the root-only policy x apps/pair-cli/config.json asset_registries.workflows.exclude — two different lists over overlapping files (pair-analyze-pr-batch.test.mjs is root-only AND every dataset *.test.mjs is install-excluded). Conflating them is the available wrong fix: config.json's list is guarded to contain only files the DATASET ships, so a root-only file can never be recorded there",
        "I3 a NEW root-only file x the enumeration — the injected regression: a fourth root-only file must make the enumeration check fail until someone records why it is there",
        "I4 C6 x C2 — an entry may stay correct as an exclusion while its stated REASON stops being true; the two are checked separately"
      ]
    }
  ],
  "fixScope": {
    "owner": "the workflow-mirror exclusion policy — the root-only file set under .claude/workflows/ and the guard that records why each is excluded",
    "mode": "test",
    "allowedPaths": []
  },
  "matrix": [
    {
      "id": "r0-7-w1",
      "kind": "witness",
      "baseline": "pass",
      "condition": "C2 + C3 + C4 — the real trees at head ed3318f4a94c4d76a11d9673d6b76f4b41ef62b1, where .claude/workflows/ holds exactly three files the dataset does not",
      "oracle": "pnpm --filter @pair/knowledge-hub exec vitest run src/tools/workflow-mirror.test.ts -t 'the enumerated root-only exclusions are exactly the files the root copy holds alone'",
      "expected": "PASSES here: the enumeration is exactly ['pair-analyze-pr-batch.js','pair-analyze-pr-batch.test.mjs','pair-contracts/cycle-coordinator.test.mjs'], the derived root-only set is the same three. DISCRIMINANCE (run on an isolated copy of this file with the cycle-coordinator entry deleted — the base's state of knowledge): FAILS, \"expected [ 'pair-analyze-pr-batch.js', ...(2) ] to deeply equal [ 'pair-analyze-pr-batch.js', ...(1) ]\" (4 of 20 tests fail).",
      "covers": [
        "r0-7"
      ],
      "rationale": "The check the prose could not make. It is the row that closes r0-7: after it, an undocumented root-only file is a red suite, not a reader's guess."
    },
    {
      "id": "r0-7-w2",
      "kind": "witness",
      "baseline": "pass",
      "condition": "C3 + C6 — the recorded reason for cycle-coordinator.test.mjs ('it cannot be mirrored: its ../../skills/pair-workflow-* imports name the INSTALLED skill layout') is checked rather than asserted: four specifiers must resolve from .claude/workflows/pair-contracts/ and from nowhere under dataset/.workflows/pair-contracts/",
      "oracle": "pnpm --filter @pair/knowledge-hub exec vitest run src/tools/workflow-mirror.test.ts -t 'an exclusion that claims unresolvable imports is checked against both locations'",
      "expected": "PASSES here (4 specifiers checked, all resolve where the file runs, none resolve from the dataset location). DISCRIMINANCE A (base copy, entry absent): FAILS, \"no exclusion backs its rationale with a resolvable/unresolvable specifier — the reason is prose again: expected 0 to be greater than 0\". DISCRIMINANCE B (a specifier added that DOES resolve from the dataset, '../pair-implement-batch.js'): FAILS, \"pair-contracts/cycle-coordinator.test.mjs: ../pair-implement-batch.js WOULD resolve from the dataset — the stated reason is stale\".",
      "covers": [
        "r0-7"
      ],
      "rationale": "The finding asks that the record say WHY the file cannot be mirrored. A prose sentence cannot be verified; these four specifiers can, against the real trees, in both directions — so the rationale expires the day it stops being true instead of outliving it."
    },
    {
      "id": "r0-7-i1",
      "kind": "interaction",
      "baseline": "pass",
      "condition": "I1 — on COPIES of the two real trees, cycle-coordinator.test.mjs is mirrored into the dataset: exactly the 'repair the drift' a reader performs when the enumerated list does not mention it",
      "oracle": "pnpm --filter @pair/knowledge-hub exec vitest run src/tools/workflow-mirror.test.ts -t 'mirroring an excluded file into the dataset is caught'",
      "expected": "PASSES here: with the file mirrored, the subset guard (shipped minus live) is [] and the two copies are byte-identical — both pre-existing guards satisfied — while the enumeration assertion throws (the file is no longer root-only, so the recorded entry is now stale). DISCRIMINANCE (base copy, entry absent): FAILS, \"expected [Function] to throw an error\" — nothing in the suite objects to the mirror, which is precisely the silence the finding describes.",
      "covers": [
        "r0-7"
      ],
      "rationale": "The failure mode named in the finding, executed rather than described. It also proves the byte guard cannot be the check: it is green in the broken state."
    },
    {
      "id": "r0-7-i2",
      "kind": "interaction",
      "baseline": "pass",
      "condition": "I3 — a FOURTH root-only file (pair-contracts/injected-regression.test.mjs) is written into an isolated copy of the real .claude/workflows/ tree",
      "oracle": "pnpm --filter @pair/knowledge-hub exec vitest run src/tools/workflow-mirror.test.ts -t 'a fourth root-only file makes the enumeration check fail'",
      "expected": "PASSES here: before the injection the copy reproduces the real verdict (the same three files); after it, the undocumented set is exactly ['pair-contracts/injected-regression.test.mjs'] and the enumeration assertion throws. DISCRIMINANCE (base copy, entry absent): FAILS at the pre-injection control, \"expected [ 'pair-analyze-pr-batch.js', ...(2) ] to deeply equal [ 'pair-analyze-pr-batch.js', ...(1) ]\".",
      "covers": [
        "r0-7"
      ],
      "rationale": "The group's stated oracle: without it, 'the enumeration equals reality' is satisfied by any list that happens to match today and nothing proves it would object tomorrow. This is the injected regression, kept permanently in the suite rather than run once."
    },
    {
      "id": "r0-7-b1",
      "kind": "boundary",
      "baseline": "pass",
      "condition": "C4 — a stale entry: 'pair-implement-batch.js' (a MIRRORED file) added to the enumeration in an isolated copy",
      "oracle": "pnpm --filter @pair/knowledge-hub exec vitest run src/tools/workflow-mirror.test.ts -t 'the enumerated root-only exclusions are exactly the files the root copy holds alone'",
      "expected": "The equality fails in the OTHER direction: \"expected [ 'pair-analyze-pr-batch.js', ...(2) ] to deeply equal [ 'pair-analyze-pr-batch.js', ...(3) ]\" (2 of 20 tests fail). At this head, with no stale entry, the row passes.",
      "covers": [
        "r0-7"
      ],
      "rationale": "An enumeration that only grows becomes folklore of its own: an entry for a file that is no longer root-only still reads as an authority. Equality, not containment, is what the row asserts."
    },
    {
      "id": "r0-7-b2",
      "kind": "boundary",
      "baseline": "pass",
      "condition": "C5 — the enumeration is complete but a rationale is too thin to act on (the cycle-coordinator entry reduced to why: 'runnability' in an isolated copy)",
      "oracle": "pnpm --filter @pair/knowledge-hub exec vitest run src/tools/workflow-mirror.test.ts -t 'every enumerated exclusion says why'",
      "expected": "FAILS in the probe: \"root-only exclusions recorded without a usable reason: pair-contracts/cycle-coordinator.test.mjs\". PASSES at this head, where every entry carries a full sentence.",
      "covers": [
        "r0-7"
      ],
      "rationale": "The finding is not 'the key is missing', it is 'the reason is missing'. A key with an empty value would satisfy w1 and leave the reader exactly where they started."
    },
    {
      "id": "r0-7-c1",
      "kind": "control",
      "baseline": "pass",
      "condition": "C7 — the agent-definitions pair: .claude/agents/ and dataset/.agents/ hold the same five files, and the pair's declared root-only set is empty",
      "oracle": "pnpm --filter @pair/knowledge-hub exec vitest run src/tools/workflow-mirror.test.ts -t 'the enumerated root-only exclusions are exactly the files the root copy holds alone'",
      "expected": "PASSES for the 'agent definitions' pair with [] on both sides — an empty record is an assertion, not an omission: a root-only agent definition would be drift with no exemption to hide behind.",
      "covers": [
        "r0-7"
      ],
      "rationale": "A positive case the policy must not break. It also shows the policy is per-pair rather than a workflows-only special case."
    },
    {
      "id": "r0-7-c2",
      "kind": "control",
      "baseline": "pass",
      "condition": "C1 + C8 — the pre-existing mirror obligations, untouched by this contract: every dataset file exists at the root, and every one of them is byte-identical",
      "oracle": "pnpm --filter @pair/knowledge-hub exec vitest run src/tools/workflow-mirror.test.ts",
      "expected": "20 passed (20) — the 13 pre-existing assertions (subset, byte identity, non-vacuity, agentType/skill reachability, the install exclude list and its staleness check, the shipped-reference guards) all still pass alongside the 7 added here.",
      "covers": [
        "r0-7"
      ],
      "rationale": "This group is guard-strength: the only acceptable effect on the existing guards is none."
    },
    {
      "id": "r0-7-n1",
      "kind": "not-applicable",
      "baseline": "pass",
      "condition": "I2 — the class 'a root-only file recorded in apps/pair-cli/config.json asset_registries.workflows.exclude'",
      "oracle": "pnpm --filter @pair/knowledge-hub exec vitest run src/tools/workflow-mirror.test.ts -t 'excludes nothing that is not there'",
      "expected": "No row of this contract asserts anything about config.json, and none is needed.",
      "covers": [
        "r0-7"
      ],
      "rationale": "Provably cannot occur, and the proof is already in the suite at base lines 211-218: that check requires every exclude entry to be a file the DATASET ships, and a root-only file by definition is not. So the two lists cannot be merged or made to stand in for one another, and the available wrong fix for r0-7 (record cycle-coordinator.test.mjs in config.json instead) is already rejected by an existing, unmodified guard. Re-derivable by reading that test."
    }
  ],
  "redTests": [
    {
      "file": "packages/knowledge-hub/src/tools/workflow-mirror.test.ts",
      "kind": "test",
      "baseline": "pass",
      "sha256": "sha256:be11d118f9b688759a124496f3a043ec2e20eba95fe0fe61231e6fdf3e7f367e",
      "command": "pnpm --filter @pair/knowledge-hub exec vitest run src/tools/workflow-mirror.test.ts",
      "observed": "src/tools/workflow-mirror.test.ts (20 tests) 121ms — Test Files 1 passed (1), Tests 20 passed (20)"
    }
  ],
  "testExempt": false,
  "reconciled": [],
  "preserved": [],
  "notes": "mode:test, allowedPaths []. Production is already CORRECT — cycle-coordinator.test.mjs genuinely belongs at the root only, and no production byte should move to close r0-7. The whole obligation lives in test source, so this contract carries the guard-strengthening itself and every row is baseline:pass; discriminance is proven per row against isolated copies of this file (entry removed = the base's state of knowledge; stale entry; thin rationale; stale specifier) and, permanently in the suite, against an injected fourth root-only file. The probe files used for the isolated runs were deleted; the worktree holds exactly one modified path."
}
