name: Probe D6b merge key inside jobs
"on":
  push:
    branches:
      - feature/US-413-ci-format-check
jobs:
  base: &base
    runs-on: ubuntu-latest
    steps:
      - name: ok
        run: echo D6B-BASE-RAN
  d6b:
    <<: *base
