name: Probe D6a alias decides the trigger
"on":
  pull_request:
    branches: &b
      - feature/US-413-ci-format-check
  push:
    branches: *b
jobs:
  d6a:
    runs-on: ubuntu-latest
    steps:
      - name: ok
        run: echo D6A-ALIAS-RESOLVED
