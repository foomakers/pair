# [Subdomain Name] — Context

> Co-located sibling of `subdomain/<slug>.md`. Created lazily when this subdomain's inline context in `context-map.md` outgrows the shared file — the maintaining session proposes the split, the human approves. Maintained inline per the [Context Map Maintenance](../../architecture/design-patterns/context-map-maintenance.md) guideline.

## Glossary

Ubiquitous language scoped to this subdomain.

| Term | Definition |
| --- | --- |
| [Term] | [What it means inside this subdomain — sharpen vague terms until they're testable] |

## Entities

Domain entities and aggregates owned by this subdomain.

| Entity | Description |
| --- | --- |
| [Entity] | [What it represents; identity, lifecycle, or ownership notes] |

## Rules and Invariants

Business rules scoped to this subdomain. Domain-wide rules stay in `context-map.md`'s Common Rules and Invariants section — don't duplicate them here.

- [Rule or invariant] — [what breaks if it's violated; cite the DDR if one formalizes it]
